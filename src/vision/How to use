1.確認資料夾裏面有新的影像程式(有web_interface , web_line, web_monitor）

2.先開啟roscore及包含策略的launch檔（記得讀取vision_better.yaml)

3.最後開啟影像程式,若先開啟影像再開策略，策略不會執行。


影像懶人包:
0.先跑到你的資料夾
1.roscore(切分頁）
2.roslaunch rosbridge_server rosbridge_web.socket（切分頁）
3.rosrun stratagy FIRA_stratagy(切分頁)
4.rosrun stratagy teamstratagy(切分頁)
5.rosrun mjpeg_server mjpeg_server(切分頁)
6.rosrun prosilica_camera prosilica_node(切分頁)
7.rosrun vision interface_node(切分頁)
8.rosrun vision white_and_black(切分頁)
9.rosrun vision monitor
如果上述程式碼沒有執行，輸入 「. devel/setup.bash」後再輸入一次

策板子懶人包：
0.進入資料夾
1.roscore(切分頁)
2.roslaunch rosbridge_server rosbridge_web.socket（切分頁）
3.rosrun motion motion
4.拉動網頁介面拉桿，看馬達有沒有正確反應。

如果上述程式碼沒有執行，輸入 「. devel/setup.bash」後再輸入一次
